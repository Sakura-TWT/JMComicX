# Changelog

## v1.0.7

完善运行日志与分享缓存治理。

- 日志行升级为结构化字段，包含时间、级别、会话、版本、用户、线程、网络、内存、前后台与当前路由。
- 增强生命周期、导航、底部 Tab、网络请求、更新检测与下载任务的诊断记录。
- 网络请求新增 requestId 串联开始、成功与失败日志，异常会记录完整堆栈。
- 新增主线程卡顿、网络切换、低内存等系统状态记录。
- 日志分享 ZIP 改为临时缓存，分享前清理旧包，分享后自动删除，避免 shared_logs 长期堆积。

## v1.0.6

新增运行日志诊断中心。

- 关于页新增红色运行日志输出开关，开启后按启停时间自动分段记录。
- 新增本地日志中心，支持单条、多选、删除和 ZIP 压缩包分享。
- 分享全部日志会打包自运行以来的所有本地日志。
- 日志记录覆盖应用生命周期、导航、网络请求、更新检测、异常与崩溃信息。
- 增加 Maven 镜像 fallback，提升国内环境构建稳定性。

## v1.0.5

修复更新安装与开关交互。

- 自动检测更新开关增加滑动、按压和颜色过渡效果。
- 关闭状态下提升开关对比度，避免与背景融为一体。
- Release 构建改用固定签名，后续 GitHub 发布包可正常覆盖安装。

## v1.0.4

优化阅读与下载入口体验。

- 章节选择弹窗会自动定位到当前阅读章节附近。
- 下载完成的漫画可从下载页直接进入阅读。
- 下载中或本地文件异常时会给出明确提示，不再出现点击无反馈。

## v1.0.3

新增应用更新检测。

- 启动时静默检查 GitHub Releases，有新版本才提示。
- 关于页增加当前版本、手动检查更新与自动检测开关。
- 增加 GitHub API 备用代理端点，降低国内网络不可达时的失败概率。

## v1.0.2

压缩体积并修复启动稳定性。

- 重新开启 release 代码压缩与资源压缩。
- 保留本地持久化模型字段，避免压缩后读取旧登录数据异常。
- 增加自动登录空值防护，异常缓存不再导致启动闪退。

## v1.0.1

发布版启动闪退修复。

- 临时关闭 release 代码压缩与资源压缩，规避启动闪退。
- 保留 release 签名，确保发布包可安装。

## v1.0.0

首个 JMX 发布版本。

- 完成项目命名与基础结构整理。
- 引入 Apple 风格与 Liquid Glass 视觉方向。
- 支持首页、搜索、详情、阅读、账号、签到与下载流程。
- 增加免责声明、项目引用与 GitHub Release 发布。
